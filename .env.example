# Local dev — talk to backend directly
# REACT_APP_BASE_URL=http://localhost:3001

# Production (Vercel) — HTTPS proxy avoids mixed-content blocking
REACT_APP_BASE_URL=https://laywer-ai.vercel.app/api
REACT_APP_API_TOKEN=

# Supabase Auth (replaces Firebase Auth)
REACT_APP_SUPABASE_URL=https://twoadfuhzukurkixjycn.supabase.co
REACT_APP_SUPABASE_ANON_KEY=
# Optional fallback only (non-browser). In the app, redirect uses window.location.origin automatically.
# REACT_APP_SUPABASE_RESET_REDIRECT=http://localhost:3000/set-password
