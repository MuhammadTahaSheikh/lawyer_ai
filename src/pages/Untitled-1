



from fastapi.middleware.cors import CORSMiddleware

# Enable CORS for your frontend to access the backend
origins = [
    "http://localhost:3000",  # Adjust this to your frontend URL if it's different
    "http://127.0.0.1:8000",
]

app.add_middleware(
    CORSMiddleware,
    allow_origins=origins,  # Allows your frontend URL
    allow_credentials=True,
    allow_methods=["*"],  # Allows all HTTP methods
    allow_headers=["*"],  # Allows all headers
)